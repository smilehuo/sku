<!--index.wxml-->
<text class='button-style' bindtap='buttonClick'>点击选择规格</text>
<block wx:if="{{propsList != ''}}">
    <specwindow id='specwindow' propsList='{{propsList}}' skuData='{{skuData}}'/>
</block>
  

